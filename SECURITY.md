# 安全策略

## 支持边界

此仓库的安全设计目标是：在**可信的本地操作员边界**内运行一个源代码仪表盘。它不支持局域网、互联网、反向代理、端口转发、公共 Webhook、公共 API 或多租户部署。请不要把这种不受支持的部署方式当作已评估的安全配置。

当前代码中的主要本地控制包括：

- 必须从启动进程环境读取非空 `GPT_REGISTER_OPERATOR_TOKEN`；
- 回环对端和规范回环 Host 限制，不信任转发请求头；
- 默认不配置 CORS，且 API 响应采用 `Cache-Control: no-store`；
- 操作员令牌可建立签名的 `HttpOnly` / `SameSite=Strict` 本地会话；基于该会话的危险请求必须通过 CSRF 校验；
- 可选的 `GPT_REGISTER_EMAIL_WEBHOOK_TOKEN` 与操作员令牌相互独立；未配置或不匹配时，邮件回调会拒绝；
- 出站 HTTP 路径保持 TLS 证书与主机名验证，不提供不安全回退。

这些陈述描述当前源码的控制点，不构成公共部署、数据加密、第三方服务可信度或外部系统安全性的保证。具体数据边界见[安全与数据处理](docs/security-and-data-handling.md)。

## 发布前的私密报告前提

此源码目录尚未连接到已配置私密报告的 GitHub 仓库，因此现在没有可验证的私密漏洞报告终点。**首次公开发布前，维护者必须**：

1. 在目标 GitHub 仓库的 **Security** 设置中启用私密漏洞报告；
2. 从仓库的 **Security** 页面确认实际的私密安全公告 URL；
3. 用该真实 URL 替换 Issue 模板中的设置链接，并更新本节；
4. 仅在该链接可用后，允许公开贡献者按此策略提交报告。

在上述条件满足前，不要通过公开 Issue、讨论、提交信息、演示视频或聊天记录披露漏洞细节，也不要把本项目描述为已提供私密报告通道。

私密通道启用后，报告应尽量提供：受影响的版本、提交或文件范围；安全影响与前提条件；可在本地隔离环境中复现的最小步骤；建议修复方向（如有）；以及可选的致谢方式。**绝不**上传真实令牌、密码、会话 cookie、验证码、账号资料、邮箱内容、代理凭据、生产配置、数据库或浏览器状态；请用明显虚构且不可用的材料替代。

## 研究规则

仅在你拥有或获得明确书面授权的本地环境、系统和数据中研究。不要将测试扩展到第三方账户、第三方 API、公共网络、真实付款流程、身份验证系统、邮件/验证码通道或他人的信息。

若必须展示请求结构，请删除可识别数据和所有密钥；不要把操作员令牌放进 URL、命令行、配置文件、前端构建变量或浏览器持久存储。

## 处理原则

维护者在已配置的私密通道收到报告后，会评估影响、最小化复现并协调修复。此政策不承诺响应时限、漏洞奖励、版本支持期限或第三方披露结果。需要公开说明时，应先确认补丁、数据清理和受影响方沟通不会扩大风险。

## 非安全支持

功能咨询、文档改进和非敏感错误应遵循[贡献指南](CONTRIBUTING.md)。关于使用边界的限制见[负责任使用](docs/responsible-use.md)。

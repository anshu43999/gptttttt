# 贡献指南

感谢你帮助改进这个**仅限可信本地操作员**的源码项目。提交前请先阅读[README](README.md)、[架构](docs/architecture.md)、[安全与数据处理](docs/security-and-data-handling.md)和[负责任使用](docs/responsible-use.md)。

## 贡献前的硬性边界

1. **不提交敏感运行资料。** 不提交令牌、密码、私钥、验证码、账号资料、浏览器状态、代理凭据、配置实值、数据库、输出、日志、抓包、截图或导出的产品数据。
2. **不扩大网络边界。** 不提交局域网/公共监听、端口转发、反向代理、公共 Webhook、放宽 CORS、信任转发头或绕过认证/CSRF 的改动。
3. **不把外部操作带进测试。** 测试与复现应使用隔离的虚构数据和本地替身；不得以真实第三方服务、真实账号或他人数据作为验证目标。
4. **不绕开服务控制。** 不贡献用于规避 CAPTCHA、反自动化、身份验证、地理/代理限制、支付限制或服务使用规则的功能或说明。
5. **不混入生成物。** 前端构建目录、依赖目录、缓存和其他可再生输出不属于源码贡献。参见 `.gitignore` 与[发布审计矩阵](docs/release-audit.md)。

## 小而可审阅的提交方式

1. 从当前源码创建一个聚焦变更；在说明中写清问题、预期行为和不包含的范围。
2. 复用已有模式，不为相同问题引入第二套配置、认证或存储约定。
3. 对受影响行为运行**聚焦且不触发真实外部操作**的检查；说明实际运行的命令与结果，不把未运行的检查描述为已通过。
4. 更新受影响的用户文档或安全说明；链接应使用仓库内相对路径并保持可解析。
5. 提交前检查差异中是否出现敏感文本、运行数据、生成物或未说明的第三方内容。

## 配置与数据约定

- `GPT_REGISTER_OPERATOR_TOKEN`、可选的独立 Webhook 密钥和会话签名密钥都只能来自**启动进程环境**；贡献中不得把它们加入 YAML、示例值、前端变量或文档输出。
- `config.example.yaml` 只是字段结构示例，不是可共享的生产配置。涉及外部系统的值必须留在操作员的本地受控环境中。
- 配置界面中的掩码是展示层辅助，不代表内容已加密或可安全提交。参见[安全与数据处理](docs/security-and-data-handling.md)。

## 来源、版权与许可证

根目录 [LICENSE](LICENSE) 表达版权持有人 35844493 对其拥有权利的第一方材料所作的 MIT 授权。贡献者在提交时确认：

- 对所提交内容拥有必要权利，或已取得可再分发的明确许可；
- 不会复制来源不明的代码、文本、二进制、图像、配置、数据集或生成物；
- 若材料含第三方成分，已在变更说明中提供来源、版权归属、适用许可证和必须保留的通知；
- 所选许可证与本仓库发布方式兼容，且不试图把第三方材料重新标为 MIT。

在来源与许可证未完成可核验说明前，维护者不得把该材料视为可发布内容。参见[第三方说明](THIRD_PARTY_NOTICES.md)。

## 安全问题与行为问题

- **安全漏洞：** 不要公开提交 Issue、补丁或利用细节。私密漏洞报告必须等待目标仓库按[SECURITY.md](SECURITY.md)启用并公布真实的私密公告终点；在此之前不得把该通道描述为可用。
- **行为问题：** 不要在公开讨论中披露不必要的个人信息。维护者必须在公开发布前提供并记录私密联络方式；在此之前，公开讨论只应包含足以请求非敏感协调的最少事实。

## 审阅标准

维护者会优先检查边界是否被保留：本地性、认证、CSRF、密钥来源、数据最小化、来源可追溯性和文档事实性。合并不表示任何第三方服务授权、条款合规、可用性或业务结果得到保证。

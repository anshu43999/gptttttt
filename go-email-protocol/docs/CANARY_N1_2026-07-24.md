# Live canary n=1 — 2026-07-24

## Result: **SUCCESS**

| Field | Value |
|---|---|
| Exit | `0` |
| Duration | ~48s (09:50:50 → 09:51:38 +08) |
| Email provider | `outlook_token` |
| Proxy | bestgo seed mint (`proxy_seed` → sticky SID, region JP) |
| Transport | `tls` (`-tags tlsclient`, Firefox profile) |
| Browser FP | Firefox 150 |
| FSM | S0→S1→S2→S3→S4→**S9**→S10→S11→S12→S13→S14 |
| `access_token` len | **1762** |
| `account_id` | `user-yXL5zaG0keGpimABunyUOYeS` |
| Import PK | `10427` |
| Output | `output/pure_go_register_canary/ChristePocasangre365_at_hotmail.com_20260724_015138.json` |
| Log | `output/pure_go_register_canary/canary_n1_otp.log` |

## Command (reproducible)

```bash
# env: GPT_REGISTER_DB_BACKEND=postgres (env.db)
cd go-email-protocol
go build -tags tlsclient -o bin/pure-go-register.exe ./cmd/pure-go-register
go run ./tools/mint_proxy bestgo,1024 JP   # → socks5 sticky URL
./bin/pure-go-register.exe \
  -mailbox-provider outlook_token \
  -proxy "$PROXY_URL" \
  -browser firefox \
  -out ../output/pure_go_register_canary \
  -timeout 15m -otp-timeout 360s -email-tries 5
```

## Failures before success (and fixes)

| Attempt | Failure | Root cause | Fix |
|---|---|---|---|
| 1 | `edge_challenge_required` @ S4 (stdlib direct) | `pure-go-register` hard-coded `DirectSOCKSFactory` | Prefer `transport.NewFactory("tls")` when built with `-tags tlsclient` |
| 2 | same @ S4 (tls) | S4 **rewrote** S3 authorize URL with fake `client_id=openai-auth-web` + new state | Use S3 `continue_url` **as-is** |
| 3 | same @ S4, but body was real OTP page | Detector treated JSD preload `/cdn-cgi/challenge-platform/` on 200 auth HTML as challenge | Require challenge status or non-auth-shell for platform signal |
| 3 body | title「受信箱を確認してください」, `passwordless_signup` | Final URL stayed on authorize; need body route | S4 body contains `email-verification` / `passwordless_signup` → **S9** |

## Evidence artifacts

- Wire dumps: `output/pure_go_register_canary/wire_*`
- Logs: `canary_n1.log`, `canary_n1_tls.log`, `canary_n1_s4fix.log`, `canary_n1_otp.log`
- Proxy dial check: `proxy_dial.txt` (ipify OK before run)

## Not claimed

- Not n=5 / n=10 / n=100
- Not software/dashboard path
- Not CF auto-solve (did not need solve; false positive fixed)
- 1024 seed currently **disabled** in DB; only bestgo seed available this run

## Next

1. n=5 same CLI path (bestgo + outlook_token + tlsclient)
2. Port S4-as-is + detector tighten + body OTP route already in original tree (this canary)
3. Only then raise concurrency
